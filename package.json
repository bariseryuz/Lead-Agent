{
  "name": "lead-agent",
  "private": true,
  "scripts": {
    "start": "bash ./start.sh",
    "build": "npm --prefix frontend ci && npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=20"
  }
}

